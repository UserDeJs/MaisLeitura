{
  "name": "maisleitura",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.0.4",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "services:up": "docker-compose -f compose.yaml up -d",
    "services:down": "docker-compose -f compose.yaml down",
    "migrate:create": "node-pg-migrate -m inframigrations create",
    "migrate:up": "node-pg-migrate -m inframigrations --envPath .env up"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "node-pg-migrate": "^8.0.3",
    "pg": "^8.16.3"
  }
}
